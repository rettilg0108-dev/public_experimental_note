# Experimental Notes

Static GitHub Pages site for password-protected experimental notes.

- `index.html`: top page
- `notes/`: encrypted note pages
- `robots.txt` and `_headers`: search-indexing hints

The note contents are encrypted in each HTML file and shown after entering the password.
